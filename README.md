# 🩺 Medical STT App (Doctor–Patient Conversation)

A full web app for recording, transcribing, and summarizing doctor–patient conversations.

## 🚀 Setup Instructions (Windows)

### 1. Backend Setup
```bash
cd backend
pip install -r requirements.txt
# Also install ffmpeg if not installed
choco install ffmpeg
python main.py
```

### 2. Frontend Setup
```bash
cd frontend
npm install
npm start
```

Open your browser at http://localhost:3000
